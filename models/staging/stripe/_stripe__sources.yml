version: 2

sources:
  - name: stripe
    description: stripe table
    database: raw
    schema: stripe
    tables:
      - name: payments
        columns: 
          - name: id
            tests:
              - not_null
              - unique